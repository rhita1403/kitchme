<div class="container margin-top-negative">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
      <% @kitchen.photos.each do |photo| %>
        <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
      <% end %>
      <div>
        <%= simple_form_for [@kitchen] do |f| %>
        <%= f.input :name, as: :string, input_html: %>
        <%= f.input :adress, as: :string, input_html: %>
        <%= f.input :capacity, as: :integer, input_html: %>
        <%= f.input :price, as: :integer, input_html: %>
        <%= f.input :equipement, as: :text, input_html: %>
        <%= f.submit %>
      <% end %>
      </div>
        </tbody>
      </table>
      </div>
    </div>
  </div>
</div>

<p><%= link_to "back to list", kitchens_path %></p>
